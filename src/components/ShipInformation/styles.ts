import { CSSProperties } from "react";

export const mainContainer: CSSProperties = {
  display: "flex",
  flexDirection: "column",
  gap: "1rem",
};

export const shipLengthHolder: CSSProperties = {
  display: "flex",
  justifyContent: "center",
};
