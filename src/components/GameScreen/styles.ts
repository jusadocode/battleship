import { CSSProperties } from "react";

export const mainContainer: CSSProperties = {
  width: "80vw",
  display: "flex",
  justifyContent: "space-between",
};

export const interactiveContainer: CSSProperties = {
  display: "flex",
  flexDirection: "column",
};

export const statisticsHeader: CSSProperties = {
  display: "flex",
};
